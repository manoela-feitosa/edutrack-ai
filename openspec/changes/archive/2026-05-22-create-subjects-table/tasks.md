## 1. Database Table Definition

- [x] 1.1 Create the table schema file `tables/subjects.xs` defining fields: `id` (int), `created_at` (timestamp), `name` (text with trim filter), `teacher` (text with trim filter), `hours` (int), and `user_id` (int referencing the `user` table).
- [x] 1.2 Add standard primary key index on `id` and sorting index on `created_at` desc.

## 2. Verification

- [x] 2.1 Verify XanoScript schema format and files using OpenSpec status tools.
